import styled from "styled-components";

const BodyChat = () => {
    return (
        <BodyWrapper>
            <h1>Body Chat</h1>
        </BodyWrapper>
    );
};

const BodyWrapper = styled.div`
  flex: 1;
  padding: 20px;
`;

export default BodyChat;